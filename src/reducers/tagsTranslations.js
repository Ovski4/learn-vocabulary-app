const tagsTranslationsReducer =  (tagsTranslations = [], action) => {

    switch (action.type) {
        default:
            return tagsTranslations;
    }
}

export default tagsTranslationsReducer;
