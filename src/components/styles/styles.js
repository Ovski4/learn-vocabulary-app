import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        flex:1,
        margin:5
    },
    revisionWords: {
        flexDirection: 'row',
        flex:0.8
    }
});
